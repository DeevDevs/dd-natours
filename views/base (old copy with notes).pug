doctype html
html 
    head
        //-we can place JS code into such bracket to make it executed within the line
        title Natours: #{tour} 
        link(rel='stylesheet', href='css/style.css')
        link(rel='shortcut icon', type='image/png', href='img/favicon.png')

    body
        h1= tour
        h2= user.toUpperCase()
        // h1 The Park Camper (this comment will be visible in the output (for users))
        
        //- this is NOT BUFFERED code. It is executed here, but it never goes to the page
        - const x = 9; 
        //- this is BUFFERED code. It goes to the page 
        h2= 2*x 
        p This is just some text
