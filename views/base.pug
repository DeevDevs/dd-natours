doctype html
html 
    head
        block head
            meta(charset='UTF-8')
            meta(name='viewport' content='width=device-width, initial-scale=1.0')
            link(rel='stylesheet', href='/css/style.css')
            link(rel='shortcut icon', type='image/png', href='/img/favicon.png')
            link(rel='stylesheet', href='https://fonts.googleapis.com/css?family=Lato:300,300i,700') 
            title Natours | #{title}
    //- Here I add dataset property to the body used ONLY in case of the booking. It will check if the alert is passed in the response, and if yes - render the alert message for the user.. it will be stored in res.locals =)
    body(data-alert=`${alert ? alert : ''}`)
        //  HEADER
        include _header
      
        // CONTENT
        block content
            h1 This is a placeholder heading

        // FOOTER
        include _footer

        //- script(defer src='https://cdnjs.cloudflare.com/ajax/libs/axios/0.27.2/axios.min.js')   
        //- script(defer src='/js/mapbox.js')   
        //- script(defer src='/js/login.js')
        script(src='https://js.stripe.com/v3/')
        script(defer src='/js/bundle.js')    

    